import { Space, Typography } from 'antd';
import styled from 'styled-components';

export const BodyContainer = styled(Space)`
  margin: 0 16px;
`;

export const BodyMessage = styled(Typography.Paragraph)`
  margin: 0 !important;
`;
